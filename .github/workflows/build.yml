name: Build Project

on:
  push:
  workflow_dispatch:

jobs:
  Build:
    strategy:
      matrix:
        generator: [ roaster, javapoet, gizmo ]
    uses: evanchooly/workflows/.github/workflows/build.yml@master
    with:
      maven-flags: -Dgenerator=${{ matrix.generator }}